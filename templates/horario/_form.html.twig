{{ form_start(form, {'attr': {'id': 'form-horario'}}) }}
    {{ form_row(form.dia) }}
    {{ form_row(form.horarioInicio) }}
    {{ form_row(form.horarioFin) }}
    {{ form_row(form.cantModulos) }}

    <button class="btn btn-primary">Guardar</button>
{{ form_end(form) }}
