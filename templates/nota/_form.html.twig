{{ form_start(form) }}
    {{ form_widget(form) }}
    <div style="text-align: center;">
        <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
    </div>
{{ form_end(form) }}
